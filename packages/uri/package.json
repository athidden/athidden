{
  "name": "@athidden/uri",
  "version": "0.1.0",
  "private": false,
  "description": "Parse and validate restricted AT URIs with support for both `at://` and `athidden://` protocols.",
  "author": "Lua <lua@foxgirl.dev> (https://foxgirl.dev/)",
  "license": "MIT",
  "repository": {
    "url": "git+https://github.com/athidden/athidden",
    "directory": "packages/uri"
  },
  "bugs": {
    "url": "https://github.com/athidden/athidden/issues"
  },
  "homepage": "https://github.com/athidden/athidden",
  "type": "module",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "bun test",
    "build": "tsc --project tsconfig.build.json",
    "prepublish": "rm -rf dist && bun run build && bun run test"
  },
  "dependencies": {
    "@atproto/syntax": "^0.5.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "latest"
  }
}
